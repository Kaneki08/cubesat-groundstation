/**
 * CubeSat Ground Station - App Entry Point
 * 
 * This is the main app entry point (imported by main.tsx).
 * The actual dashboard logic lives in pages/Dashboard.tsx
 */

import Dashboard from "./pages/Dashboard";

export default function App() {
  return <Dashboard />;
}
